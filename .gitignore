
# 本機排程用的套件vendor資料夾，不需要跟著網站一起部署
scripts/vendor/

# 2026-09-16：帳密/API金鑰/私人筆記類的txt檔案，放在repo根目錄純粹是方便自己
# 找（跟著Google雲端硬碟同步），但絕對不能進版控——只加 /*.txt（只擋根目錄），
# 不擋 scripts/vendor/ 底下套件本身附的 LICENSE.txt / top_level.txt 之類檔案。
# 之後又新增新的帳密筆記檔案，一樣會被這條規則自動擋掉，不用每次手動加。
/*.txt
